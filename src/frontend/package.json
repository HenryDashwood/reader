{
  "name": "reader-frontend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel ./*.html && cp .env dist/.env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Henry Dashwood",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "parcel": "^2.7.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "path-browserify": "^1.0.1"
  },
  "browserslist": [
    "last 1 chrome versions"
  ]
}
